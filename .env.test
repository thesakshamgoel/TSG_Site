# TEST-ONLY mode: blank the Firebase keys so the site runs in local mode
# (no auth gating), used to exercise form + console feedback paths in CI/headless.
# Loaded after .env.local when building with: npm run build -- --mode test
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
